---
framework: mocha
preRun:
     command:
        - npm install
preMerge:
  pattern:
    - "./test/*.cjs"
postMerge:
  pattern: 
    - "./test/*.cjs"
postRun:
     command:
        - node --version
configFile: mocharc.yml
nodeVersion: 12.13.0
version: 1.0
